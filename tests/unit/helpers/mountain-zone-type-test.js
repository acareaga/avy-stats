import { mountainZoneType } from 'first-tracks/helpers/mountain-zone-type';
import { module, test } from 'qunit';

module('Unit | Helper | mountain zone type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = mountainZoneType([42]);
  assert.ok(result);
});
